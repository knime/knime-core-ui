name: CI

on:
  pull_request:
  push:
    branches:
      - master
      - "releases/**"

jobs:
  lint-test-build:
    name: ${{ matrix.package }} - Lint, tests, and build
    permissions:
      contents: read
    runs-on: ubuntu-latest
    strategy:
      matrix:
        package: [core-ui, scripting-editor]
    defaults:
      run:
        working-directory: js-src/packages/${{ matrix.package }}
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
        with:
          package_json_file: js-src/package.json
      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: js-src/package.json
          cache: pnpm
          cache-dependency-path: js-src/pnpm-lock.yaml
      - name: Install dependencies
        working-directory: js-src
        run: pnpm install
      - name: Audit
        working-directory: js-src
        run: pnpm run audit
      - name: Type check
        run: pnpm run type-check
      - name: Lint and format check
        run: pnpm run ci:lint-format
      - name: Run coverage
        run: pnpm run coverage
      - name: Build
        run: pnpm run build